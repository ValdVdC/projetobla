import { BackCorDirective } from './back-cor.directive';

describe('BackCorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackCorDirective();
    expect(directive).toBeTruthy();
  });
});
